let i=0;const slides=[...document.querySelectorAll(".slide")],counter=document.getElementById("counter");function show(n){i=(n+slides.length)%slides.length;slides.forEach((s,k)=>s.classList.toggle("active",k===i));counter.textContent=(i+1)+" / "+slides.length;window.scrollTo({top:0,behavior:"smooth"})}function next(){show(i+1)}function prev(){show(i-1)}function reveal(btn){btn.style.display="none";btn.nextElementSibling.style.display="block"}document.addEventListener("keydown",e=>{if(e.key==="ArrowRight"||e.key===" "){e.preventDefault();next()}if(e.key==="ArrowLeft")prev()});show(0);